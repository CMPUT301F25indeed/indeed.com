<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="#000000"
    android:padding="18dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="@drawable/bg_admin_image_card"
        android:padding="20dp">

        <!-- Title -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Waitlist"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="#FFFFFF"
            android:layout_marginBottom="12dp" />

        <!-- Invite Entrants Button -->
        <Button
            android:id="@+id/waitlistpopup_inviteEntrants_Button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_button_primary"
            android:text="Invite Entrants"
            android:textAllCaps="false"
            android:textColor="@android:color/white"
            android:layout_marginBottom="16dp" />

        <!-- LIST AREA: fixed height so 2–3 names visible -->
        <ListView
            android:id="@+id/waitlistpopup_listview"
            android:layout_width="match_parent"
            android:layout_height="260dp"
            android:divider="#333333"
            android:dividerHeight="1dp"
            android:paddingTop="6dp"
            android:paddingBottom="6dp" />

    </LinearLayout>

</ScrollView>
