<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingStart="16dp"
    android:paddingEnd="16dp"
    android:background="#000000">

    <!-- LEFT TITLE -->
    <TextView
        android:id="@+id/Organizer_UpcomingBanner"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="40dp"
        android:text="My Upcoming Events"
        android:textSize="32sp"
        android:textColor="#FFFFFF"
        android:fontFamily="@font/ibmplexserif_semibold"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toStartOf="@id/Organizer_Upcoming_NewEventButton" />

        <Button
            android:id="@+id/Organizer_Upcoming_NewEventButton"
            android:layout_marginTop="40dp"
            android:layout_width="0dp"
            android:layout_height="37dp"
            android:layout_marginBottom="10dp"
            android:background="@drawable/bg_button_primary"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:text="+ New Event"
            android:textAllCaps="false"
            android:textColor="#FFFFFF" />

    <!-- DIVIDER -->
<View
android:id="@+id/Organizer_Divider"
android:layout_width="0dp"
android:layout_height="1dp"
android:layout_marginTop="20dp"
android:background="#333333"
app:layout_constraintTop_toBottomOf="@id/Organizer_UpcomingBanner"
app:layout_constraintStart_toStartOf="parent"
app:layout_constraintEnd_toEndOf="parent"/>

    <!-- EVENT LIST -->
    <ListView
        android:id="@+id/Organizer_UpcomingEventList"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="16dp"
        android:divider="@android:color/transparent"
        android:dividerHeight="12dp"
        app:layout_constraintTop_toBottomOf="@id/Organizer_Divider"
        app:layout_constraintBottom_toTopOf="@id/Organizer_Upcoming_HomeButton"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>


    <!-- HOME BUTTON -->
    <Button
        android:id="@+id/Organizer_Upcoming_HomeButton"
        android:layout_width="314dp"
        android:layout_height="40dp"
        android:layout_marginBottom="28dp"
        android:background="@drawable/bg_button_primary"
        android:text="Home"
        android:textAllCaps="false"
        android:textColor="#FFFFFF"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.492"
        app:layout_constraintStart_toStartOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>
