<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#000000"
    android:paddingTop="32dp"
    android:paddingLeft="16dp"
    android:paddingRight="16dp"
    android:paddingBottom="16dp">

    <!-- Top row: Back button + Title -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingBottom="16dp">

        <Button
            android:id="@+id/admin_logs_back"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_alignParentStart="true"
            android:text="←"
            android:textColor="#FFFFFF"
            android:background="@drawable/bg_button_danger_border" />

        <TextView
            android:id="@+id/admin_logs_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="System Logs"
            android:textSize="24sp"
            android:textStyle="bold"
            android:fontFamily="@font/opensans"
            android:textColor="#FFFFFF"
            android:layout_centerHorizontal="true"
            android:layout_centerVertical="true" />
    </RelativeLayout>

    <!-- Subtitle -->
    <TextView
        android:id="@+id/admin_logs_subtitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Search, filter, and review notifications."
        android:textColor="#BBBBBB"
        android:textSize="16sp"
        android:layout_marginBottom="12dp"
        android:gravity="center"
        android:fontFamily="@font/opensans" />

    <!-- Search bar -->
    <SearchView
        android:id="@+id/admin_logs_search"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#111111"
        android:queryHint="Search logs by sender, action, or date"
        android:layout_marginBottom="12dp" />

    <!-- Dropdown Filter -->
    <Spinner
        android:id="@+id/admin_logs_filter"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:background="#111111"
        android:padding="10dp"
        android:layout_marginBottom="16dp"
        android:spinnerMode="dropdown" />

    <!-- RecyclerView for notifications/logs -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/admin_logs_recycler"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:paddingTop="4dp"
        android:paddingBottom="4dp" />

</LinearLayout>
